version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Setup VirtualEnv
          command: |
            virtualenv helloworld
            . helloworld/bin/activate
            python -m pip install --upgrade pip
            pip install --no-cache-dir -r requirements.txt
      - run:
          name: Run Tests
          command: |
            . helloworld/bin/activate
            python test_hello_world.py

  deploy:
    machine:
      enabled: true
    working_directory: ~/repo
    steps:
      - checkout
      - add_ssh_keys: 
          fingerprints:
            - "a0:d1:0c:c4:98:b7:68:4c:9a:e1:7c:17:c1:8d:e3:94"
      - run:
          name: Installing...
          command: |
            sudo apt-get update
            sudo apt-get install sshpass -y
            sudo apt-get install openvpn
      - run:
          name: Fix ssh
          command: |
            ssh-keyscan -H 192.168.178.46  >> ~/.ssh/known_hosts 
      - run:
          name: Deploy master
          command: |
            set -e
            hostname
            hostname -I
            pwd
            ls -la /home/circleci/.ssh
            # echo "config:" && cat /home/circleci/.ssh/config
            # echo "authorized_keys:" && cat /home/circleci/.ssh/authorized_keys
            # echo "known_hosts:" && cat /home/circleci/.ssh/known_hosts
            ls -la /home/circleci/repo
            sudo -b openvpn --config /home/circleci/repo/vpn/e.semeraro@212.184.167.99.ovpn --auth-user-pass /home/circleci/repo/vpn/login.txt --daemon
            ssh k2@192.168.126.89 -vvv
            # sshpass -p 'mYBerryp1' ssh pi@192.168.178.46

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build:
          filters:
            branches:
              only:
                - master
      - deploy:
          requires:
            - build
          filters:
            branches:
              only:
                - master