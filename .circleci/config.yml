version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Setup VirtualEnv
          command: |
            virtualenv helloworld
            . helloworld/bin/activate
            python -m pip install --upgrade pip
            pip install --no-cache-dir -r requirements.txt
      - run:
          name: Run Tests
          command: |
            . helloworld/bin/activate
            python test_hello_world.py

  deploy:
    machine:
      enabled: true
    working_directory: ~/repo
    steps:
      - checkout
      - add_ssh_keys: 
          fingerprints:
            - "a0:d1:0c:c4:98:b7:68:4c:9a:e1:7c:17:c1:8d:e3:94"
      - run:
          name: Install sshpass
          command: |
            sudo apt-get update
            sudo apt-get install sshpass -y
      # - run:
      #     name: Fix ssh
      #     command: |
      #       ssh-keyscan -H 192.168.178.46  >> ~/.ssh/known_hosts 
      - run:
          name: Deploy master
          command: |
            hostname
            hostname -I
            pwd
            ls -la /home/circleci/.ssh
            echo "authorized_keys:" && cat /home/circleci/.ssh/authorized_keys
            echo "known_hosts:" && cat /home/circleci/.ssh/known_hosts
            ls -la /home/circleci/repo
            # sshpass -p 'mYBerryp1' ssh pi@192.168.178.46
            sshpass -p '123456' ssh k2@192.168.126.89

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build:
          filters:
            branches:
              only:
                - master
      - deploy:
          requires:
            - build
          filters:
            branches:
              only:
                - master